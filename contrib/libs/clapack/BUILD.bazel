load("@rules_cc//cc:defs.bzl", "cc_library", "cc_binary", "cc_test", "cc_import")

# CLAPACK — C-language LAPACK for linear algebra (text/embedding features).

cc_library(
    name = "clapack",
    srcs = glob([
        "*.c",
    ], allow_empty = True),
    hdrs = glob(["*.h", "include/*.h"], allow_empty = True),
    includes = [".", "include", "../libf2c", "../cblas"],
    deps = ["//contrib/libs/libf2c", "//contrib/libs/cblas"],
    copts = ["-Wno-everything"],
    visibility = ["//visibility:public"],
)
